/**
 * Re-exports currentUser from Clerk. For static export build, build-for-pages.cjs
 * temporarily replaces this file with currentUser-pages.ts (stub only).
 */
export { currentUser } from '@clerk/nextjs/server';
